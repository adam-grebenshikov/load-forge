# LoadForge

Distributed load testing platform.

## Quick start

```bash
pnpm install
pnpm docker:up
```

**Редактор (VS Code):** настройки лежат в `config/vscode/`

```bash
ln -sfn config/vscode .vscode
```
